/*
package Zad3;

import java.util.TreeMap;

//DLACZEGO TA PETLA NIE DZIALA?

public class Counter {

    public void countOccurences(TreeMap<Integer, Integer> myMap) {

        int count = 0;

        for (int i = 0; i < myMap.size(); i++) {
            for (int j = (myMap.size() - 1); j == 0; j--)
                if (myMap.get(j) == myMap.get(i)) {
                    count++;
                }
            System.out.println(myMap.get(i) + " - liczba wystapień: " + count);
        }
    }
}

*/
